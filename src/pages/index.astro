---
// Component imports
import { Layout } from "@globals"
import Navbar from "@component/Navbar.astro"
import Content from "@component/Content.astro"

import heroImage from "../assets/img/test.png"
import SiteInfo from "@component/SiteInfo.astro"
import { seoConfig } from "@util/seoConfig"
const siteTitle = "BgiBola"
const SiteDescription =
	"NobarTV | BGiBola | Situs Live Streaming Nonton Bola Online Gratis"

const siteLogo = "/bgibola-logo.png"
const finalTitle = siteTitle + " - " + SiteDescription
const ogTitle = siteTitle + ": " + SiteDescription
const image = {
	url: siteLogo,
	alt: siteTitle
}
---

<Layout title={finalTitle}>
	<Navbar />

	<SiteInfo seoConfig={seoConfig} />
	<br />
	<Content />
</Layout>
